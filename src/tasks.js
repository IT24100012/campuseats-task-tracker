"// CampusEats task list" 
git add src/tasks.jsconst tasks = [
    "Design the menu screen",
    "Build the orders API",
    "Add user login"
];
console.log(`CampusEats has ${tasks.length} open tasks`);